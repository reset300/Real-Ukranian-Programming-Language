# Output

`вивести` prints one or more expressions.

```text
вивести("Привіт")
вивести("Число:", число)
```

Arguments are separated by commas. The interpreter inserts one space between output arguments and writes a newline at the end.
